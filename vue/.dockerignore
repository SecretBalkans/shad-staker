# Exclude Node.js modules (dependencies)
node_modules/

# Exclude development-specific files and directories
.git/
.gitignore
npm-debug.log
yarn-error.log
*.log
*.log.*
*.swp
.vscode/
.idea/
.DS_Store
Dockerfile

# Exclude build output (if applicable)
dist/

# Exclude environment files (configurations)
.env
.env.local
.env.production
.env.staging
.dockerignore

# Exclude any other files or directories not needed in the Docker image
Here's an explanation of what each rule does:

node_modules/: Excludes the Node.js modules, which should be installed separately within t
